@'
# Core (essential)
pandas==2.1.0
numpy==1.24.3
python-dotenv==1.0.0

# Time handling
pytz==2023.3

# Data collection
requests==2.31.0
websocket-client==1.6.1

# Visualization (install later if needed)
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.17.0

# Development (optional)
jupyter==1.0.0
pytest==7.4.0
'@ | Out-File -FilePath requirements.txt -Encoding utf8 -Force